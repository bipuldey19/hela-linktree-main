# Prefer npm; avoid Next.js invoking yarn during lockfile patch.
# Build script sets NEXT_IGNORE_INCORRECT_LOCKFILE=1 to skip lockfile patching (avoids EACCES with yarn on server).
engine-strict=false
